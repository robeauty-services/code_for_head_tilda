# code_for_head_tilda

Скрипти для `<head>` сторінок Тільди. Два сайти, два незалежні бандли з одного репо.

Критичне, щоб не забути:

- **Ніколи не push напряму в `main`.** Гілка → PR → CI (`.github/workflows/ci.yml`) зелений → squash-мердж.
- Два бандли: `src/head.js` → `dist/head.min.js` (robeauty.me) і `src/manera-head.js` → `dist/manera.min.js` (maneraparfum.com). Номери секцій у двох файлах збігаються, щоб їх можна було діфати між собою.
- `dist/` комітиться в репо — саме звідти jsDelivr роздає файл. Змінив `src/` — обовʼязково перебілди (`npm run build:robeauty` / `build:manera`, або `node scripts/bump.mjs <site> <version>`) у тому ж коміті.
- `node scripts/bump.mjs` без аргументів показує, на який тег запінений кожен сайт — швидка перевірка, чи не розʼїхалось.
- Автоматичного деплою нема: після мерджу треба поставити тег (`robeauty-vX.Y.Z` / `manera-vX.Y.Z`) і вручну підняти версію в jsDelivr-URL у налаштуваннях сайту на Тільді.
- **Ніколи не переміщуй уже випущений тег** — jsDelivr кешує `@tag` назавжди, зміни просто не доїдуть (або доїдуть частково, що гірше).
