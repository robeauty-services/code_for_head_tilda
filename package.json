{
  "name": "tilda_head",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:robeauty": "terser src/head.js -c -m --format comments=false -o dist/head.min.js",
    "build:manera": "terser src/manera-head.js -c -m --format comments=false -o dist/manera.min.js",
    "build:pl": "terser src/pl-head.js -c -m --format comments=false -o dist/pl.min.js",
    "pins": "node scripts/release.mjs --list",
    "release:robeauty": "node scripts/release.mjs robeauty",
    "release:manera": "node scripts/release.mjs manera",
    "release:pl": "node scripts/release.mjs pl"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "terser": "^5.49.0"
  }
}
